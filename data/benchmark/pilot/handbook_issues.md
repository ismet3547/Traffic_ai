# Mini Pilot Handbook Issue Log

Pilot: `mini-pilot-001`

Frozen ontology: `pilot-1`

Frozen handbook: `1.0`

Record questions here; do not resolve them only in annotator memory. If a resolution changes annotation semantics, bump the handbook version, record affected clips, and re-review those clips before mixing results.

| Issue ID | Clip/time | Ambiguous question | Provisional handling | Resolution/version | Clips requiring re-review |
|---|---|---|---|---|---|
| None recorded | N/A | Real annotation has not started | N/A | N/A | N/A |

Assign stable IDs such as `HI-001`. A `FirstAgreementReviewDocument` whose action
is `HANDBOOK_REVISION` must link at least one recorded issue ID.

Prompts to watch for include overtaking-end boundaries, reasonable opportunity to return right, time between two overtakes, merges, target identity, congestion interpretation, and occlusion sufficient for `insufficient_evidence`.
